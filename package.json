{
  "name": "file-list-generator",
  "version": "1.0.0",
  "description": "A simple utility to generate list of files in a directory",
  "main": "dist/fileListGenerator.js",
  "type": "module",
  "bin": {
    "deduplicator": "./app.ts"
  },
  "scripts": {
    "build": "tsc",
    "start": "node --loader ts-node/esm fileListGenerator.ts",
    "prepare": "npm run build"
  },
  "dependencies": {
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  }
}
